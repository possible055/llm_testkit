# LLM TestKit 環境變數範例
# 
# 使用方式：
#   1. 複製此檔案：cp .env.example .env
#   2. 編輯 .env 檔案，填入實際的值
#   3. .env 檔案已被 .gitignore 排除，不會被提交到版本控制
#
# 注意：環境變數會覆蓋配置檔中的對應設定

# ============================================================================
# API 配置
# ============================================================================

# OpenAI API 金鑰（必要）
# 用於呼叫 OpenAI 相容的 API
# 當配置檔中 api_key 設為 null 時，會自動從此環境變數讀取
OPENAI_API_KEY=your-api-key-here

# OpenAI API 基礎 URL（可選）
# 預設使用 OpenAI 官方端點 (https://api.openai.com/v1)
# 若使用第三方相容 API（如 Azure OpenAI、本地部署等），需設定此變數
# 範例：
#   - Azure OpenAI: https://your-resource.openai.azure.com/
#   - 本地部署: http://localhost:8000/v1
#   - 第三方代理: https://api.example.com/v1
# OPENAI_BASE_URL=https://api.example.com/v1

# ============================================================================
# 日誌配置
# ============================================================================

# 日誌等級（可選）
# 控制日誌輸出的詳細程度
# 可選值：DEBUG, INFO, WARNING, ERROR, CRITICAL
# 預設：INFO
# LOG_LEVEL=INFO

# ============================================================================
# Hugging Face 配置（用於分詞器下載）
# ============================================================================

# Hugging Face 鏡像站（可選）
# 若分詞器下載速度慢或無法連接 Hugging Face Hub，可設定鏡像站
# 範例：
#   - 中國鏡像: https://hf-mirror.com
# HF_ENDPOINT=https://hf-mirror.com

# ============================================================================
# 審計模組專用配置
# ============================================================================

# 注意：審計模組主要透過 YAML 配置檔設定參數
# 環境變數主要用於敏感資訊（如 API 金鑰）
# 
# 配置檔範例：configs/audit_example.yaml
# 執行命令：llm-testkit audit --config configs/audit_example.yaml

